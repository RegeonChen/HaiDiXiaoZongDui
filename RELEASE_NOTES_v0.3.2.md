# 聚合拾遗 v0.3.2 Release Notes

发布日期：2026-07-31

Git Tag：`v0.3.2`

本次更新集中完善首次使用、灵活窗口和阅读器体验，同时完成 AI Provider 凭证安全存储与 Electron 工具链升级，并修复摘要 Markdown、文章标签和多处窄窗口排版问题。

## 主要更新

### 首次启动新手教程

- 首次启动时提供 8 步聚光引导，依次介绍订阅源、添加入口、文章目录、同步、阅读区、目录切换、AI 和搜索。
- 完成或跳过后持久化状态，后续启动不再自动弹出；可从设置中随时重新查看。
- 引导期间持续跟踪窗口缩放、滚动和布局变化，目标暂时缺失时自动等待或前进。
- 合并坐标测量与渲染更新，避免聚光框追逐旧位置，提升动画流畅度。

### 灵活窗口与阅读控制

- 普通单击使用可替换的预览标签；再次点击同一入口、双击或在窗口内发生有效交互后自动固定。
- 文章、设置、标签等页面中的按钮、链接、表单输入和面板调整均会保留当前窗口，避免已操作内容被替换。
- 阅读区新增滚动后出现的回到文章开头图标，并适配“减少动态效果”偏好。
- 移除译文顶部重复的返回原文按钮，翻译显示与隐藏统一由工具栏控制。

### 摘要、标签与笔记排版

- 摘要移入阅读区底部栏，与标签和笔记共享统一的 tab、拉伸、切换和收起交互。
- 摘要 Markdown 支持 1–6 级标题、有序/无序嵌套列表、引用、分隔线和 fenced code block，并保持 HTML 转义与链接协议限制。
- 中栏标签位于文章标题下方、来源和时间上方；阅读区标签位于标题下方，两处均会按可用宽度换行。
- 新建、删除或更新标签后立即刷新当前文章列表，不再等待切换订阅源。
- 修复笔记页超长文章标题撑宽灵活窗口的问题。

### 主题与界面精简

- 纸质暖黄主题扩展到完整界面，并为底部栏控件补齐暖灰层级。
- Windows 和 Linux 窗口移除 Electron 默认的 File/Edit/View 菜单栏，避免与应用工具栏形成重复顶部区域。
- 移除设置中的本地日志入口；Main 进程仍保留脱敏轮转日志供开发排查。
- 修复标签页面重复点击导致列表被清空、批量选择按钮在部分选中状态下语义错误等问题。

### 凭证与工具链安全升级

- AI Provider API Key 使用 Electron `safeStorage` 加密后写入 SQLite，并在安全后端可用时幂等迁移历史明文。
- Renderer 仍只能获得 `apiKeySet` 状态，解密后的 Key 只在 Main 进程调用 Provider 时短暂存在。
- macOS 使用 Keychain、Windows 使用 DPAPI；Linux 检测到不安全的 `basic_text` 后端时拒绝保存新 Key。
- Electron 升级至 43，electron-vite 升级至 5，Vite 升级至 7，Vitest 升级至 4，electron-builder 升级至 26.15.7。

## 验证

- TypeScript Main/Renderer 双端检查通过。
- 149 项单元测试通过；6 项真实网络 Feed 测试按设计跳过。
- `npm audit --omit=dev` 为 0 项已知生产依赖漏洞。
- Electron 正式构建及 macOS Apple Silicon DMG 本地打包通过。
- 基础隔离、UI/IPC、Feed/OPML、凭证、集成、摘要/翻译、标签、阅读模式、AI 对话、图片代理、搜索分页、工作台和新手教程等关键 smoke 通过。

## 安装包

- macOS Apple Silicon：`Juhe-Shiyi-0.3.2-arm64.dmg`
- Windows x64：`Juhe-Shiyi-Setup-0.3.2-x64.exe`

## 已知限制

- macOS 安装包未进行 Apple Developer ID 签名和公证，仅适合课程展示、开发测试和团队内部分发。
- Windows 安装包未使用商业代码签名证书，首次运行可能出现系统安全提示。
- Linux 安装包不在本次 Release 范围内。
- 真实 AI 生成依赖用户自行配置兼容 OpenAI API 的 Provider，自动化测试不会携带真实密钥。
