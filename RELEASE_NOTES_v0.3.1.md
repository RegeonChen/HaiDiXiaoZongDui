# 聚合拾遗 v0.3.1 Release Notes

发布日期：2026-07-29
Git Tag：`v0.3.1`

本次更新集中完成了阅读器工作台重构、文章上下文 AI 对话、订阅源操作闭环和跨平台应用图标，同时补强了内容清洗、本地日志与交互状态的一致性。

## 主要更新

### IDE 风格阅读工作台

- 固定为“功能栏 / 一级订阅源目录 / 二级文章目录 / 灵活窗口”四段布局。
- 标签、笔记、文摘、专题和设置只切换最右工作区，不再替换订阅源与文章目录。
- 灵活窗口支持可切换、可关闭的 IDE 标签页。
- 阅读功能键支持“全开 → 仅二级目录 → 仅灵活窗口 → 全开”三级循环。
- 系统字号与文章正文字号独立调整。

### 文章上下文 AI 对话

- 在阅读区内围绕当前文章发起多轮问答。
- 主进程使用 Cleaned Markdown 构造可追溯的文章上下文。
- 支持划选正文后直接翻译或询问。
- Provider 未配置、响应失败和上下文缺失均提供明确错误状态。

### 列表、搜索与订阅源操作

- 文章列表滚动到底自动加载，每页 50 篇，并保留加载更多兜底。
- 搜索结果直接携带完整文章对象，不再受当前分页缓存限制。
- “所有订阅源”和单个订阅源均提供作用范围明确的“同步 / 全部已读”操作。
- 同步失败提示展示具体来源和错误原因。
- 删除确认、未读计数和批量已读均改用数据库精确计数。

### 标签与 OPML 闭环

- 标签管理升级为标签列表与关联文章双栏界面。
- 文章标题和阅读区实时显示彩色标签。
- OPML 导出支持逐项选择、全选/取消全选和失败后原地重试。
- 切换订阅源、关闭文章或删除来源时同步清理旧文章状态，避免界面串数据。

### 内容呈现与本地诊断

- 复杂表格、任务列表、描述列表和动态 Markdown 代码围栏得到更稳定的清洗与渲染。
- 修复普通正文被错误识别成整段代码的问题。
- 将 `fast-xml-parser` 升级到 5.10.1，修复重复 DOCTYPE 可能绕过实体展开限制的问题。
- 本地日志以脱敏 JSONL 保存并按 2 MB 轮转，可在设置工作区查看和导出。
- 已读、星标和同步写入失败会明确反馈，不再先更新界面后静默失败。

### 新应用图标

- 采用象牙米黄竹纸奏章、朱红印章与流苏设计。
- 增加透明圆角安全区，并针对打包、运行时窗口和 favicon 输出不同尺寸。
- macOS DMG 使用 ICNS，Windows EXE 内嵌 16–256 像素多档 ICO。
- 新增图标生成和格式/透明度自动校验。

## 验证

- TypeScript Main/Renderer 双端检查通过。
- 130 项单元测试通过；6 项真实网络 Feed 测试按设计跳过。
- `npm audit --omit=dev` 为 0 项已知漏洞。
- Electron 正式构建通过。
- 基础安全、UI/IPC、Phase 2、集成、文章分页、AI 对话、订阅源操作、OPML 和工作台关键 smoke 均通过。
- macOS Apple Silicon DMG 与 Windows x64 NSIS 安装包均完成本地打包和资源核验。

## 安装包

- macOS Apple Silicon：`Juhe-Shiyi-0.3.1-arm64.dmg`
- Windows x64：`Juhe-Shiyi-Setup-0.3.1-x64.exe`

## 已知限制

- macOS 安装包未进行 Apple Developer ID 签名和公证，仅适合课程展示、开发测试和团队内部分发。
- Windows 安装包未使用商业代码签名证书，首次运行可能出现系统安全提示。
- Linux 安装包不在本次 Release 范围内。
- AI Provider 的 API Key 仍以明文保存在本地 SQLite；后续版本计划迁移到 Electron `safeStorage`。
- 完整开发工具链审计仍报告 Electron 31、Vite 5 与 electron-builder 的传递依赖公告，需要在后续版本中进行跨大版本升级和兼容性回归。
- 真实 AI 生成依赖用户自行配置兼容 OpenAI API 的 Provider，自动化测试不会携带真实密钥。
