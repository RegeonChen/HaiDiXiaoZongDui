# 聚合拾遗 v0.3.4 Release Notes

发布日期：2026-08-02

Git Tag：`v0.3.4`

本次补丁更新重点完善文章页专题创建、AI Provider 兼容性、OPML 导出操作和订阅源分组交互，让常用流程更稳定、命名更清晰。

## 主要更新

### AI 推荐专题名

- 从文章阅读区创建专题时，AI 会基于标题、来源、摘要和最佳可用正文生成 4 组“主题 + 观察维度”候选。
- 首项推荐自动填入专题名称、描述和关键词，其余候选可一键切换，也可以重新生成。
- 推荐结果按文章内容签名缓存；文章内容变化后自动失效，避免重复消耗模型额度。
- 没有可用 Provider 或生成失败时保留可编辑的本地草案，不阻断用户创建专题。

### AI Provider 兼容与错误体验

- 支持从夹带分析文字、Markdown 代码围栏、尾逗号等常见非标准模型响应中安全提取推荐 JSON。
- 完全无结构时追加一次受控修复请求；Provider 不支持 `response_format` 时自动降级重试。
- 专题推荐界面不再展示内部错误码，改为可理解、可重试的用户提示。
- “默认”推荐徽标固定单行显示，推荐卡片在窄窗口下自动重排，不再挤压或截断名称。

### OPML 与订阅源分组交互

- OPML 选择性导出页把“取消导出 / 确认导出”移到“全选 / 取消全选”旁边，长列表无需滚到底部操作。
- “添加订阅源组”统一改为“新建订阅源组”，对话框主按钮同步改为“新建”。
- 在订阅源分组区域的剩余空白处右键，可直接打开“新建订阅源组”菜单；订阅源行、分组标题和批量管理原有操作保持不变。
- 全新安装且订阅源数据库为空时，自动在“个人博客”组加入 `Soul Hacker` 默认订阅源；已有订阅或二次启动不会重复插入。

### 顶栏精简

- 移除顶栏重复的“聚合拾遗”文字，为搜索框和右侧工具保留更多横向空间。
- 搜索框调整到顶栏最左侧，并重新分配三段式顶栏宽度，搜索入口更直接。
- 新手教程从 8 步扩展到 12 步，新增标签、笔记、文摘和专题入口介绍；同步步骤改为高亮完整操作栏，同时覆盖“同步”和“全部已读”。

### 安全与发布稳定性

- 将 `sanitize-html` 升级到 `2.17.6`，修复 URI scheme 校验相关安全公告；生产依赖与完整依赖审计均为 0 项已知漏洞。
- 更新基础 UI smoke 的宽度契约，使其分别验证四段式工作台的外层目录和内层编辑区，避免继续按旧三栏 DOM 结构误报。

## 验证

- TypeScript Main/Renderer 双端检查通过。
- 160 项单元测试通过；6 项真实网络 Feed 测试按设计跳过。
- 应用图标格式、尺寸和透明通道校验通过。
- Electron 生产构建和本地 macOS Apple Silicon DMG 打包通过。
- 24 个独立 Electron/IPC smoke 全部通过，覆盖基础隔离、UI/IPC、AI Provider、专题推荐、OPML 导出、订阅源分组、工作台布局和新手教程。
- 生产依赖审计为 0 项已知漏洞。

## 安装包

- macOS Apple Silicon：`Juhe-Shiyi-0.3.4-arm64.dmg`
- Windows x64：`Juhe-Shiyi-Setup-0.3.4-x64.exe`

## 已知限制

- macOS 安装包未进行 Apple Developer ID 签名和公证，仅适合课程展示、开发测试和团队内部分发。
- Windows 安装包未使用商业代码签名证书，首次运行可能出现系统安全提示。
- Linux 安装包不在本次 Release 范围内。

## 回退方式

- 本版本没有数据库 Schema 迁移；如需回退，可重新安装 GitHub Release 中的 `v0.3.3` 安装包。
