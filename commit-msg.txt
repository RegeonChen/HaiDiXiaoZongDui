fix(ui): 侧栏按 tag 真分类 + AI 标签建议 toggle 修复

Bug 1 侧栏 tab=tags 之前一直显示"标签管理（Phase 3 落地）"占位
- 新增 ARTICLE_COUNTS_BY_TAG IPC 通道 + ArticleRepository.countByTag()
  按 article_tags 表 SQL 聚合每个 tag 名下的真实文章数
- ArticleRepository.list 加 tagIds 过滤(EXISTS 子查询 AND 语义)
- IpcDataSource / MockDataSource articles filter 加 tagIds 支持
- MockDataSource 真正持久化 articleTagMap(article ↔ tag),
  让 tagAddToArticle / tagRemoveFromArticle / tagGetByArticle 在 mock
  模式下也能跨组件共享状态
- FeedList 接收 tags / tagCounts / onSelectTag,tab=tags 渲染真 tag
  列表(每个 tag 显示 # 颜色 + 标签名 + 文章数 badge),点击 → onSelect(`tag:${id}`)
- App.tsx refreshTags + refreshTagCounts 初次 + juhe:refresh 时拉取
  真实数据,删掉之前用 useMemo 强行填 0 的临时方案
- selection.feedId 模板字面量类型 `tag:${string}` + isTagSelector
  / parseTagSelector 辅助函数,文章过滤 useEffect 增加 tag: 分支
- filterLabel 切到 tag 过滤时显示 "# 标签名"

Bug 2 AI 标签建议 toggle 失效(关闭时再次建议 / 显示时重调 AI)
- 根因:useCallback 闭包内的 stickyTab 在快速点击 / React state
  batching 时陈旧,导致 suggestOpen 判断错误;另外 StickyBottomPanel
  onTabChange 内联回调自动调 handleSuggestTags 也会形成双调
- 修复:用 stickyTabRef 替代 useCallback 闭包内的 stickyTab,每次
  handleSuggestTags 都读最新值;同时清理 onTabChange 的双调,让
  工具栏按钮 onClick 是 AI 触发唯一入口
- handleSuggestTags deps 移除 stickyTab,避免 useCallback 频繁重建

测试
- 新增 scripts/smoke-taglist.cjs + npm run smoke:taglist(9 项关键检查)
- smoke:taglist 11/11 pass + 81 单元测试 pass + 其它 12 个 smoke
  全部无回归(smoke / smoke:ui / smoke:db / smoke:phase2 /
  smoke:ui-ipc / smoke:phase2.5 / smoke:integration / smoke:topic /
  smoke:summary / smoke:inline-trans / smoke:inline-trans:split-error /
  smoke:coexist / smoke:tagmanage / smoke:reader-modes / smoke:taglist)
- typecheck:node + typecheck:web 全部通过
