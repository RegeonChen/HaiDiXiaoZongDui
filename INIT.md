# 聚合拾遗 (RSS 阅读器)

本项目将开发一款受 Mercury 启发的跨平台、本地优先桌面 RSS 阅读器，软件命名为**"聚合拾遗"**。目标用户是需要持续关注多个信息来源、阅读长篇内容并整理知识的学生、研究者和知识工作者。

应用不要求用户注册产品账号，也不依赖自有云服务。订阅源、文章、阅读状态、笔记、标签和 AI 生成结果均保存在本地。用户需要使用 AI 功能时，可以自行连接大语言模型服务。

本项目的特色功能是专题追踪。应用不只单独处理每篇文章，还会将不同订阅源中的相关报道组织到同一专题中，持续展示专题的发展过程，并生成每项结论都可以追溯到原文的多源简报。

## 功能

### 订阅源管理

- 添加订阅源
    - 解析 RSS、Atom 和 JSON Feed，或通过 OPML 批量导入和导出订阅源
    - 手动同步全部订阅源或单个订阅源
    - 支持定时同步，并清楚显示同步失败信息
    - 在导入和同步过程中避免重复订阅源和重复文章
- 编辑、删除、分组和搜索订阅源

### 阅读与内容清洗

- 阅读界面
    - 使用包含订阅源侧栏、文章列表和阅读区的桌面布局
    - 展示文章信息、已读/未读状态和星标状态
    - 正确呈现图片、表格、列表、链接、引用和代码块
    - 提供亮色与暗色主题，并允许调整字号和阅读宽度
    - 支持文章搜索，以及按订阅源、阅读状态、星标和标签筛选文章

- 内容清洗
    - 提取文章正文，去除广告、导航和其他干扰内容，并根据提取后的正文生成 Cleaned HTML 和 Cleaned Markdown
    - 允许用户在原始网页和清洗后的阅读内容之间切换

### AI 辅助阅读

- 配置 AI 模型
    - 允许用户配置兼容 OpenAI 接口的 Provider，包括 Base URL、API Key 和模型名称
    - 支持多组 Provider 和模型配置，并可以随时切换
- 辅助功能
    - 生成文章摘要（允许选择输出语言和详细程度）
    - 在保留段落结构的情况下翻译整篇文章
    - 提供原文与译文按段落对照的双语阅读模式
    - 允许用户在适当情况下调整摘要、翻译和标签 Prompt
- 调试功能
    - 在本地缓存 AI 结果，并清楚显示请求进度和错误信息

### 笔记与文摘

- 笔记/摘录的创建
    - 通过选择文章文字创建摘录
    - 为文章或摘录添加 Markdown 笔记
    - 保留摘录、笔记与原始文章及来源 URL 的关联
    - 将一篇或多篇文章中的笔记组合并排序为文摘
- 导出功能
    - 导出带笔记的单篇文章，或导出由多篇文章组成的文摘
    - 以 Markdown 作为主要可编辑导出格式，并支持 HTML 和 PDF

### 标签

- 添加标签
    - 手动为文章添加或移除一个或多个标签
    - 使用 Tag Agent 根据文章内容推荐标签，并允许用户审核
- 标签管理
    - 在标签管理界面中创建、重命名、合并和删除标签
    - 对一组选中的文章批量添加标签
    - 跨订阅源按标签筛选文章

### 专题追踪与多源简报

- 允许用户通过名称、描述和可选关键词定义专题
- 将新同步的文章匹配到相关专题
- 将同一事件的相似报道分组，减少重复阅读
- 按事件、子话题、来源和发布时间组织相关文章
- 通过时间线展示专题的发展过程
- 比较不同来源的共同结论和不同观点
- 突出相对于早期报道新增的信息
- 生成专题简报，并将每项结论链接到支持它的原始文章
- 允许用户编辑和导出专题简报

### 辅助功能

- 提供完整的中文和英文界面，并允许用户切换语言
- 提供本地日志、日志查看、日志导出和用于开发及问题反馈的调试工具
- 支持在 Windows、macOS 和 Linux 上运行和验证

## 开发说明

### 技术说明

#### 整体架构

- 使用 Electron 开发跨平台桌面应用，前端和本地后端主要使用 TypeScript

#### 前端技术

- 前端运行在 Electron Renderer 进程中，使用 React、TypeScript、HTML 和 CSS 构建
- 前端与本地后端通过类型明确的 preload 和 IPC 接口通信，Renderer 不得直接访问 SQLite 或完整的 Node.js 能力

#### 后端技术

- 本地后端运行在 Electron 的 Node.js Main 进程中，负责 Feed 请求、内容清洗、SQLite 访问、AI 调用、文件操作和本地日志
- 使用成熟的库完成 Feed 解析、正文提取、HTML 安全清洗和 HTML 到 Markdown 转换，不自行重复实现已有工具

#### 数据存储

- 使用 SQLite 和 SQL 保存订阅源、文章、阅读状态、笔记、标签、设置和 AI 结果，并通过受控的数据访问层操作数据库

#### 外部通信

- 通过 HTTP 访问外部 RSS 和大语言模型服务，通过本地文件接口处理 OPML 和文档导入导出

#### AI 设计

- 通过统一接口封装大语言模型 Provider，使用户可以更换 Provider 和模型，而不影响应用功能
- 将来源追溯作为 AI 数据结构和处理流程的一部分，而不是可选的界面展示功能

#### 安全

- 使用操作系统提供的安全能力保存 API 凭证，不得将凭证写入源码或明文配置文件

### 约束

- 提供简洁、一致且易用的阅读体验
- 无需注册、登录或强制订阅即可在本地使用
- 不添加用户行为分析、遥测或自动收集用户行为和文章内容的功能
- 只有在用户启用相关 AI 功能时，才可以将必要的文章内容发送给用户配置的模型服务
- 核心功能和本地数据不得依赖单一操作系统
- AI 功能不得绑定特定模型厂商
- 外部文章内容必须经过提取和安全清洗后才能显示
- 应用日志不得包含 API Key 或其他凭证
- 通过项目文档和 Git 历史保留重要决策、Coding Agent 工作、个人贡献和团队协作记录

### 开发环境

- Node.js LTS
- Electron
- React + TypeScript
- SQLite
- Git 和 GitHub
- Windows / macOS / Linux
